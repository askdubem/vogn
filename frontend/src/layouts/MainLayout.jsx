// Main layout — wraps all public pages
// Will include: Navbar + children + Footer + WhatsApp button
const MainLayout = ({ children }) => {
  return <div className="main-layout">{children}</div>;
};
export default MainLayout;
